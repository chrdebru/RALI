PROJECT {NO_VEHICULE} (SELECT NOM="Terieur" AND PRENOM="Alain" (PERSONNE) JOIN VOYAGE JOIN AVION)

PROJECT {NOM, PRENOM} (SELECT PERMIS_AVION=0 (CHAUFFEUR) JOIN VOYAGE JOIN AVION JOIN PERSONNE)

PROJECT {NOM, PRENOM} (PROJECT {NO_VOYAGE} (RENAME NO_DESTINATION<-NO_DEST_TO (SELECT LIBELLE="Honolulu" (DESTINATION)) JOIN VOYAGE JOIN TRAIN) JOIN ABORD JOIN PASSAGER JOIN PERSONNE)
